        # Boton DashBoard
        self.cmdDash = tk.Button(
            self.sidebar,
            bg = sidebar_color,
            font = font_awesome,
            text=' DashBoard',
            #image = self.IconDash,
            #command = self.toggle_panel,
            bd = 0,
            #bg = topbar_color,
            fg = "white"
            )
        #self.cmdDash.pack(side = tk.TOP)
        
        #  Boton Perfil
        self.cmdProfile = tk.Button(
            self.sidebar,
            bg = sidebar_color,
            font = font_awesome,
            text='Perfil',
            #image = self.IconCuenta,
            #command = self.toggle_panel,
            bd = 0,
            #bg = topbar_color,
            fg = "white"
            )
       # self.cmdProfile.pack(side = tk.TOP)
        
        # Boton Picture
        self.cmdPicture = tk.Button(
            self.sidebar,
            bg = sidebar_color,
            font = font_awesome,
            text='Picture',
            #image = self.IconLogo,
            #command = self.toggle_panel,
            bd = 0,
            #bg = topbar_color,
            fg = "white"
            )
        #self.cmdPicture.pack(side = tk.TOP)
        
        # Boton Info
        self.cmdInfo = tk.Button(
            self.sidebar,
            bg = sidebar_color,
            font = font_awesome,
            text='Informacion',
            #image = self.IconInfo,
            #command = self.toggle_panel,
            bd = 0,
            #bg = topbar_color,
            fg = "white"
            )
        #self.cmdInfo.pack(side = tk.TOP)
        
        # Boton Settings
        self.cmdSetting = tk.Button(
            self.sidebar,
            bg = sidebar_color,
            font = font_awesome,
            text='Configuracion',
            #image = self.IconCfg,
            #command = self.toggle_panel,
            bd = 0,
            #bg = topbar_color,
            fg = "white"
            )          
        #self.cmdSetting.pack(side = tk.TOP)